#include <stdio.h>
/* betty style doc for function main goes there */
/**
 * main- main start
 * @main - for alphabet
 * Return: Always 0 (Success)
 */

int main(void)
{
	int ch;

	ch = '_';
	putchar(ch);
	ch = 'p';
	putchar(ch);
	ch = 'u';
	putchar(ch);
	ch = 't';
	putchar(ch);
	ch = 'c';
	putchar(ch);
	ch = 'h';
	putchar(ch);
	ch = 'a';
	putchar(ch);
	ch = 'r';
	putchar(ch);
	putchar('\n');

	
}
