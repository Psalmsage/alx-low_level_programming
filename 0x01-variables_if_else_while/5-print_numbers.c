#include <stdio.h>
/* betty style doc for function main goes there */
/**
 * main- main start
 * @main - for alphabet
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
		printf("%d", n);
	putchar('\n');
	return (0);
}
