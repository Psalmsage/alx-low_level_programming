#include <stdio.h>
#include "main.h"

/**
 * main- main start
 * @argc: for arguments
 * @argv: for all
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	(void) argv;

	printf("%i\n", argc - 1);

	return (0);
}
